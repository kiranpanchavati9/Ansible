# Minimal Ansible Configuration (`ansible.cfg`)

This file contains only the required configuration for Ansible inventory locations.

---

## Configuration

```ini
[defaults]
inventory=['/etc/ansible/host', '/root/list']
